class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        # arr = len(nums)
        # for i in range(arr):
        #     nums.append(nums[i])
        return nums + nums
        